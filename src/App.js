import React, { Component } from "react";
import { Route, Switch } from "react-router";

const AppStyle = {
  height: "100%",
  width: "100%",
  background: "#ddd"
};

class App extends Component {
  render() {
    return <div style={AppStyle}>Testing</div>;
  }
}

export default App;
